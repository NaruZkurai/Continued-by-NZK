const $ = sel => document.querySelector(sel);
const $$ = sel => document.querySelectorAll(sel);
const $$$(sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const id = (s, i) => s + (i || "");
const el = (cls, txt) => document.createElement("div"); cls ? el(null, "").classList.add(cls) : el();
const elb = (cls, txt) => document.createElement("button"); cls ? elb(null, "").classList.add(cls) : elb();
elb.className = "primary"; elb.innerText = txt; elb.onclick = () => { };

const $c = sel => document.querySelector(sel);
const $$c = sel => document.querySelectorAll(sel);
const $$$c = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elc = (cls, txt) => document.createElement("div"); cls ? elc(null, "").classList.add(cls) : elc();
const elcb = (cls, txt) => document.createElement("button"); cls ? elcb(null, "").classList.add(cls) : elcb();
elcb.className = "primary"; elcb.innerText = txt; elcb.onclick = () => { };

const $d = sel => document.querySelector(sel);
const $$d = sel => document.querySelectorAll(sel);
const $$$d = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const eld = (cls, txt) => document.createElement("div"); cls ? eld(null, "").classList.add(cls) : eld();
const eldb = (cls, txt) => document.createElement("button"); cls ? eldb(null, "").classList.add(cls) : eldb();
eldb.className = "primary"; eldb.innerText = txt; eldb.onclick = () => { };

const $e = sel => document.querySelector(sel);
const $$e = sel => document.querySelectorAll(sel);
const $$$e = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const ele = (cls, txt) => document.createElement("div"); cls ? ele(null, "").classList.add(cls) : ele();
const elbe = (cls, txt) => document.createElement("button"); cls ? elbe(null, "").classList.add(cls) : elbe();
elbe.className = "primary"; elbe.innerText = txt; elbe.onclick = () => { };

const $f = sel => document.querySelector(sel);
const $$f = sel => document.querySelectorAll(sel);
const $$$f = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elf = (cls, txt) => document.createElement("div"); cls ? elf(null, "").classList.add(cls) : elf();
const elbf = (cls, txt) => document.createElement("button"); cls ? elbf(null, "").classList.add(cls) : elbf();
elbf.className = "primary"; elbf.innerText = txt; elbf.onclick = () => { };

const $g = sel => document.querySelector(sel);
const $$g = sel => document.querySelectorAll(sel);
const $$$g = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elg = (cls, txt) => document.createElement("div"); cls ? elg(null, "").classList.add(cls) : elg();
const elbg = (cls, txt) => document.createElement("button"); cls ? elbg(null, "").classList.add(cls) : elbg();
elbg.className = "primary"; elbg.innerText = txt; elbg.onclick = () => { };

const $h = sel => document.querySelector(sel);
const $$h = sel => document.querySelectorAll(sel);
const $$$h = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elh = (cls, txt) => document.createElement("div"); cls ? elh(null, "").classList.add(cls) : elh();
const elbh = (cls, txt) => document.createElement("button"); cls ? elbh(null, "").classList.add(cls) : elbh();
elbh.className = "primary"; elbh.innerText = txt; elbh.onclick = () => { };

const $i = sel => document.querySelector(sel);
const $$i = sel => document.querySelectorAll(sel);
const $$$i = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const eli = (cls, txt) => document.createElement("div"); cls ? eli(null, "").classList.add(cls) : eli();
const elbi = (cls, txt) => document.createElement("button"); cls ? elbi(null, "").classList.add(cls) : elbi();
elbi.className = "primary"; elbi.innerText = txt; elbi.onclick = () => { };

const $j = sel => document.querySelector(sel);
const $$j = sel => document.querySelectorAll(sel);
const $$$j = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elj = (cls, txt) => document.createElement("div"); cls ? elj(null, "").classList.add(cls) : elj();
const elbj = (cls, txt) => document.createElement("button"); cls ? elbj(null, "").classList.add(cls) : elbj();
elbj.className = "primary"; elbj.innerText = txt; elbj.onclick = () => { };

const $k = sel => document.querySelector(sel);
const $$k = sel => document.querySelectorAll(sel);
const $$$k = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elk = (cls, txt) => document.createElement("div"); cls ? elk(null, "").classList.add(cls) : elk();
const elbk = (cls, txt) => document.createElement("button"); cls ? elbk(null, "").classList.add(cls) : elbk();
elbk.className = "primary"; elbk.innerText = txt; elbk.onclick = () => { };

const $l = sel => document.querySelector(sel);
const $$l = sel => document.querySelectorAll(sel);
const $$$l = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const ell = (cls, txt) => document.createElement("div"); cls ? ell(null, "").classList.add(cls) : ell();
const elbl = (cls, txt) => document.createElement("button"); cls ? elbl(null, "").classList.add(cls) : elbl();
elbl.className = "primary"; elbl.innerText = txt; elbl.onclick = () => { };

const $m = sel => document.querySelector(sel);
const $$m = sel => document.querySelectorAll(sel);
const $$$m = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elm = (cls, txt) => document.createElement("div"); cls ? elm(null, "").classList.add(cls) : elm();
const elbm = (cls, txt) => document.createElement("button"); cls ? elbm(null, "").classList.add(cls) : elbm();
elbm.className = "primary"; elbm.innerText = txt; elbm.onclick = () => { };

const $n = sel => document.querySelector(sel);
const $$n = sel => document.querySelectorAll(sel);
const $$$n = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const eln = (cls, txt) => document.createElement("div"); cls ? eln(null, "").classList.add(cls) : eln();
const elbn = (cls, txt) => document.createElement("button"); cls ? elbn(null, "").classList.add(cls) : elbn();
elbn.className = "primary"; elbn.innerText = txt; elbn.onclick = () => { };

const $o = sel => document.querySelector(sel);
const $$o = sel => document.querySelectorAll(sel);
const $$$o = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elo = (cls, txt) => document.createElement("div"); cls ? elo(null, "").classList.add(cls) : elo();
const elbo = (cls, txt) => document.createElement("button"); cls ? elbo(null, "").classList.add(cls) : elbo();
elbo.className = "primary"; elbo.innerText = txt; elbo.onclick = () => { };

const $p = sel => document.querySelector(sel);
const $$p = sel => document.querySelectorAll(sel);
const $$$p = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elp = (cls, txt) => document.createElement("div"); cls ? elp(null, "").classList.add(cls) : elp();
const elbp = (cls, txt) => document.createElement("button"); cls ? elbp(null, "").classList.add(cls) : elbp();
elbp.className = "primary"; elbp.innerText = txt; elbp.onclick = () => { };

const $q = sel => document.querySelector(sel);
const $$q = sel => document.querySelectorAll(sel);
const $$$q = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elq = (cls, txt) => document.createElement("div"); cls ? elq(null, "").classList.add(cls) : elq();
const elbq = (cls, txt) => document.createElement("button"); cls ? elbq(null, "").classList.add(cls) : elbq();
elbq.className = "primary"; elbq.innerText = txt; elbq.onclick = () => { };

const $r = sel => document.querySelector(sel);
const $$r = sel => document.querySelectorAll(sel);
const $$$r = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elr = (cls, txt) => document.createElement("div"); cls ? elr(null, "").classList.add(cls) : elr();
const elbr = (cls, txt) => document.createElement("button"); cls ? elbr(null, "").classList.add(cls) : elbr();
elbr.className = "primary"; elbr.innerText = txt; elbr.onclick = () => { };

const $s = sel => document.querySelector(sel);
const $$s = sel => document.querySelectorAll(sel);
const $$$s = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const els = (cls, txt) => document.createElement("div"); cls ? els(null, "").classList.add(cls) : els();
const elbs = (cls, txt) => document.createElement("button"); cls ? elbs(null, "").classList.add(cls) : elbs();
elbs.className = "primary"; elbs.innerText = txt; elbs.onclick = () => { };

const $t = sel => document.querySelector(sel);
const $$t = sel => document.querySelectorAll(sel);
const $$$t = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elt = (cls, txt) => document.createElement("div"); cls ? elt(null, "").classList.add(cls) : elt();
const elbt = (cls, txt) => document.createElement("button"); cls ? elbt(null, "").classList.add(cls) : elbt();
elbt.className = "primary"; elbt.innerText = txt; elbt.onclick = () => { };

const $u = sel => document.querySelector(sel);
const $$u = sel => document.querySelectorAll(sel);
const $$$u = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elu = (cls, txt) => document.createElement("div"); cls ? elu(null, "").classList.add(cls) : elu();
const elbu = (cls, txt) => document.createElement("button"); cls ? elbu(null, "").classList.add(cls) : elbu();
elbu.className = "primary"; elbu.innerText = txt; elbu.onclick = () => { };

const $v = sel => document.querySelector(sel);
const $$v = sel => document.querySelectorAll(sel);
const $$$v = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elv = (cls, txt) => document.createElement("div"); cls ? elv(null, "").classList.add(cls) : elv();
const elbv = (cls, txt) => document.createElement("button"); cls ? elbv(null, "").classList.add(cls) : elbv();
elbv.className = "primary"; elbv.innerText = txt; elbv.onclick = () => { };

const $w = sel => document.querySelector(sel);
const $$w = sel => document.querySelectorAll(sel);
const $$$w = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elw = (cls, txt) => document.createElement("div"); cls ? elw(null, "").classList.add(cls) : elw();
const elbw = (cls, txt) => document.createElement("button"); cls ? elbw(null, "").classList.add(cls) : elbw();
elbw.className = "primary"; elbw.innerText = txt; elbw.onclick = () => { };

const $x = sel => document.querySelector(sel);
const $$x = sel => document.querySelectorAll(sel);
const $$$x = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elx = (cls, txt) => document.createElement("div"); cls ? elx(null, "").classList.add(cls) : elx();
const elbx = (cls, txt) => document.createElement("button"); cls ? elbx(null, "").classList.add(cls) : elbx();
elbx.className = "primary"; elbx.innerText = txt; elbx.onclick = () => { };

const $y = sel => document.querySelector(sel);
const $$y = sel => document.querySelectorAll(sel);
const $$$y = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const ely = (cls, txt) => document.createElement("div"); cls ? ely(null, "").classList.add(cls) : ely();
const elby = (cls, txt) => document.createElement("button"); cls ? elby(null, "").classList.add(cls) : elby();
elby.className = "primary"; elby.innerText = txt; elby.onclick = () => { };

const $z = sel => document.querySelector(sel);
const $$z = sel => document.querySelectorAll(sel);
const $$$z = (sel, cls) => { const n = parseInt(cls); return Array.from($$(`.${sel}`).children).slice(0, n); };
const elz = (cls, txt) => document.createElement("div"); cls ? elz(null, "").classList.add(cls) : elz();
const elbz = (cls, txt) => document.createElement("button"); cls ? elbz(null, "").classList.add(cls) : elbz();
elbz.className = "primary"; elbz.innerText = txt; elbz.onclick = () => { };

console.log("file-mover loaded");
"